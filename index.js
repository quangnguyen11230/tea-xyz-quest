// In ra "Xin chào!"
console.log("Xin chào!");

// Biến lưu trữ tên
const name = "Bard";

// In ra "Xin chào, Bard!"
console.log(`Xin chào, ${name}!`);

// Hàm tính tổng hai số
function sum(a, b) {
  return a + b;
}

// In ra tổng của 10 và 20
console.log(sum(10, 20)); // 30

// Vòng lặp in ra các số từ 1 đến 5
for (let i = 1; i <= 5; i++) {
  console.log(i);
}

// Đối tượng lưu trữ thông tin về một người
const person = {
  name: "Bard",
  age: 1,
  city: "Incheon",
};

// In ra tên và tuổi của người
console.log(`${person.name} - ${person.age}`);
